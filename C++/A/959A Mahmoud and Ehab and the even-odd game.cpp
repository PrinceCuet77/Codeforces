#include <bits/stdc++.h>
using namespace std;

int main()
{
        //freopen("file.txt", "r", stdin);
        long long n;
        while ( cin >> n ) {
                if ( n % 2 == 0 )
                        cout << "Mahmoud" << endl;
                else
                        cout << "Ehab" << endl;
        }

        return 0;
}
