#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

int main()
{
        //freopen("file.txt", "r", stdin);
        ll t;
        scanf("%lld", &t);
        while ( t-- ) {
                ll a, b, c, d;
                scanf("%lld %lld %lld %lld", &a, &b, &c, &d);

                printf("%lld %lld %lld\n", a, c, c);
        }

        return 0;
}
